// let age = 6
// let isChild = age <= 7
// let isSenior = age >= 65
// let hasDiscount = isChild || isSenior

// console.log(hasDiscount)

let age = 67

if(age <= 7)
    console.log('It is a child, so it has discount')
else if(age >= 65)
    console.log('It is a senior, so it has discount')
else
    console.log('It is not a child nor a senior, so it does not have discount')